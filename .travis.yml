language: c
install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq check protobuf-c-compiler libprotobuf-c0-dev libjson-c-dev
  - gem install ronn
before_script:
  - autoreconf -i
script:
  - ./configure --enable-silent-rules
  - make
  - make check
compiler:
  - gcc
  - clang
